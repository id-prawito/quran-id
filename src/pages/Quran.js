import React from "react";
import ThemeSwitcher from "../config/theme/ThemeSwitcher";

const Quran = () => {
    return (
        <div>
            <ThemeSwitcher />
            <h1>
                React Redux, React Loading, Styled Components, React Icons,
                sass, React Router Dom, Github Pages, Coba, Bisa
            </h1>
        </div>
    );
};

export default Quran;
